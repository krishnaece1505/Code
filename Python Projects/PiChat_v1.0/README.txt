#-----------------------------------------------------------------
PiChat is a simple chat application that allows communication between a PC (client) and the Raspberry Pi (server). 
The communication is established through a TCP connection and it makes use of the "SOCKET" module in Python.
The code was tested with Python 2.7.
#-----------------------------------------------------------------
